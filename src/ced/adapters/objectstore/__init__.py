"""Object store adapter — content-addressed payload read/write.

Public surface: ``client`` for raw dict/bytes payloads and ``history`` for
message-history serialization with reasoning strip. Import from the submodule
that owns the name; this ``__init__`` is a namespace marker only.
"""
