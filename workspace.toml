# workspace.toml
#
# Repository coordination index. Canonical artifacts own requirements; this
# file records lifecycle membership, source provenance, display summaries, and
# hard dependencies.
#
# Target entries are inline tables with exactly:
#   path, kind, source, summary, needs
#
# Example:
#   { path = "docs/specs/<slug>/spec.md", kind = "spec", source = { mode = "repo-origin" }, summary = "Example spec", needs = [] }
#
# Comments, summaries, list order, tracker labels, and profile hints are
# non-semantic. They must not determine routing, dependency satisfaction,
# processor selection, or dispatch.
#
# Paths are repository-relative POSIX paths. Consumers must reject absolute
# paths, backslashes, ".." segments, and any symlink-resolved target outside the
# repository root.

[backlog]
open = [
  { path = "src/ced/adapters/postgres/roles.py", kind = "defect", source = { mode = "repo-origin" }, summary = "ceiling-element-unchecked-yields-no-refusal-event: decode_role_record judges agent_role.ceiling for array-ness only, so a non-mapping entry loads. _ceiling_pins then skips it, `quarantined = not ceiling` in compiler.py reads it as a real binding, and _bound_integrations reaches entry.get() on it as an AttributeError that append_role_refusal cannot classify into role.load.failed or role.compile.refused — so a stored-record integrity violation produces no refusal event at all, in a system whose event log is its inspection surface. Not a privilege defect: ceiling is operator-authored configuration behind a database-write privilege app_api, app_worker and app_policy do not hold, and _bound_integrations runs before the agent is assembled, so an entry that wrongly sets quarantined = False always dies before producing an over-privileged agent. The loss is the classification of the refusal, not the refusal. Remedy not determined: refusing a non-mapping entry at the decode seam with a message naming the row and the position is the obvious one, and it needs a criterion for the same reason the tools element rule did. Surfaced by the T4 security and quality reviews 2026-09-22, after the decision to leave ceiling out of the tools-element change was taken and without this consequence on the table. Owner eugenelim", needs = [] },
  { path = "docs/product/intents/evidence-backed-fund-diligence.md", kind = "intent", source = { mode = "repo-origin" }, summary = "Deferred second demonstration domain: managed funds and ETFs; gated on an RFC amending the charter, not buildable", needs = [] },
  { path = "docs/product/intents/assistant-mediated-operation.md", kind = "intent", source = { mode = "repo-origin" }, summary = "Draft capability intent: conversational control of the desk, bounded by the same authority and provenance as a run. Framed 2026-09-18 from a journey map and service blueprint; nine gaps named, two structural", needs = [] },
  { path = ".github/pull_request_template.md", kind = "defect", source = { mode = "repo-origin" }, summary = "pre-existing-no-top-level-directory: test_no_top_level_directory_is_unrecorded in tests/architecture/test_recorded_layout.py fails on main. `.github` arrived 2026-09-20 in 9e20d97, postdating ADR-0003, so D2's exemption for predating entries misses it and PREDATING cannot carry it; D2's amendment requirement applies. 9e20d97 claims the owner waived the RFC for this file, which AGENTS.md contradicts. Record or retract that waiver. Ask-first", needs = [] },
  { path = "pyproject.toml", kind = "defect", source = { mode = "repo-origin" }, summary = "No dependency or secret scanning exists: AGENTS.md states there is no CI, and neither pip-audit nor an image or secret scanner is wired, so CVEs in the pydantic-ai-slim[bedrock] pin, hypothesis and publicsuffix2 have no owner. Surfaced by the spec-stage security review 2026-09-20; re-confirmed unaddressed by the round-4 security review 2026-09-21", needs = [] },
  { path = "docs/adr/0006-four-r5-deviations-for-phase-1.md", kind = "defect", source = { mode = "repo-origin" }, summary = "ADR-0006 D1 and D2 return conditions, carried as follow-ons in walking-skeleton-step-lifecycle rather than as criteria. D1 returns when the Bedrock IAM shape for bedrock:CountTokens against a geo-prefixed model id is re-derived and count_tokens_before_request can be enabled in production; that spec then gains a criterion that a per-request token bound refuses a request under production wiring. D2 returns when the object-store write path exists so a hash can be written as well as read; that spec then gains a criterion that an instruction hash resolves from the event log alone. Identifiers AC-0271 and AC-0272 were allocated to these on 2026-09-21 and withdrawn the same day, and are not reallocated: an unchecked acceptance criterion is a HARD lint violation at a Shipped transition, so the follow-on carrier is the one spec-and-plan-contract.md prescribes. Owner eugenelim", needs = [] },
  { path = "docs/specs/walking-skeleton-step-lifecycle/plan.md", kind = "defect", source = { mode = "repo-origin" }, summary = "Content-addressing provenance has no criterion because Phase 1 mints no such value. walking-skeleton-role-compilation's AC-0274 third clause refuses every content-addressing value outright and records the general rule, that such a value carries AC-0221's minting-and-provenance test whichever class a ratified document files it under. That rule is assertable only once a minting source exists, and the source is the payload-object write path walking-skeleton-step-lifecycle opens. The spec that opens it adds the provenance criterion and must change AC-0274's categorical refusal case rather than inherit a green test that never checked provenance. Surfaced by the round-5 and round-6 security and adversarial reviews 2026-09-21. Owner eugenelim", needs = [] },
  { path = "docs/specs/walking-skeleton-step-lifecycle/spec.md", kind = "defect", source = { mode = "repo-origin" }, summary = "Which guard refused a role compilation is not recoverable from the event log. walking-skeleton-role-compilation's AC-0261 asserts only what migration 0001's events envelope can carry: that a compile refusal is distinguishable by type from a runtime fault and names the failing role. Naming the specific guard needs either a column on events or a payload object, and that spec writes no payload object and does not amend a foundation-owned table. Becomes buildable when walking-skeleton-step-lifecycle opens the payload-object write path. Owner eugenelim", needs = [] },
  { path = ".claude/skills/new-spec/references/spec-and-plan-contract.md", kind = "defect", source = { mode = "repo-origin" }, summary = "no-lifecycle-route-for-widening-an-approved-unsealed-contract: adding an acceptance criterion to a spec that is Approved but whose baseline is not sealed fits no documented transition, and the route taken on 2026-09-22 for walking-skeleton-step-lifecycle was the closest available rather than a sanctioned one. Three texts, each verified verbatim. spec-and-plan-contract.md lines 43-45: specs are living documents until the plan is approved, and from approval onward the correction is the controlled-amendment path, not an in-flight edit. delivery-contract-lifecycle.md line 77: that amendment event is unavailable in spec-plan mode and outside CODE-IMPLEMENTATION, which a spec with no engine run cannot be in. delivery-contract-lifecycle.md lines 22-31: the hand reset of Approved to Draft and Drafting is the rejected-gate recovery, and no gate was rejected. So the three routes are mutually exclusive here and none fits: living-document editing has ended at approval, the amendment transition needs a state the spec cannot occupy, and the reset misuses a recovery for a rejection that did not happen. The gap is specific to the window between plan approval and plan-locked, which exists because new-spec approves a spec and plan while the work-loop engine seals the baseline later and separately. It is not hypothetical: that window is exactly where adding a criterion is cheapest, so it is where an author has most reason to be there. What was done instead, recorded so the precedent is visible rather than silent: the pair was hand-reset to Draft and Drafting, the criteria added, and re-approval left owed to the owner. Not a decision and no remedy proposed - this entry records the conflict only, per AGENTS.md section Scoped instructions, which says to report conflicting instructions rather than work around them. Whichever of the two skill references owns the rule is where a remedy would land. Surfaced during the walking-skeleton-step-lifecycle criterion amendment 2026-09-22. Ask-first. Owner eugenelim", needs = [] },
  { path = "docs/specs/walking-skeleton-step-lifecycle/plan.md", kind = "defect", source = { mode = "repo-origin" }, summary = "thinking-disable-not-demonstrated-on-a-run-path: on pydantic-ai 2.45.0 AC-0204's compiled thinking=False does not reach the provider. Obligation: the value the model reads on the executor's run path is False. The canonical account of which layers defeat it lives in docs/specs/walking-skeleton-step-lifecycle/spec.md section Assumptions and is deliberately not copied here, because four copies of it had already drifted apart by the first review; that account is recorded as the layers found rather than a complete set, since a spec-stage security review added one to it after the first draft. Now carried as contract: walking-skeleton-step-lifecycle gained two criteria. AC-0275 refuses at compile a role whose resolved model would send no provider-level disable, owned by its T6; AC-0276 refuses any call reaching a provider on the same predicate over the rendered request, sited at the model seam because the framework merges agent, capability and per-run settings before calling the model, and owned by its T7. The observable is the rendered request and deliberately not ModelRequestParameters.thinking, which is False while an adaptive profile renders nothing. Two criteria because the seams differ - compiler and model seam - and each needs its own mutation proof. A third, a boot-time refusal, was drafted as AC-0277 and withdrawn on 2026-09-22 with its identifier not reallocated; that spec's Follow-ons records why and its return condition. Placed there rather than as an amendment to walking-skeleton-role-compilation because that spec is Shipped with no amendment transition available, its own Follow-ons routes the controls to the spec that first issues a provider call, and the adapter half needs an adapter it does not ship. Stays open because the code defect is unfixed: no guard refuses a model id today. Phase 1 admits only the Claude families where the unified setting reaches the wire, by owner decision of 2026-09-22, which excludes claude-sonnet-4-6, claude-opus-4-6 and later on Bedrock and is recorded with its return condition in that spec's Follow-ons. Closes when AC-0275 and AC-0276 are green with the per-half mutation proof T6 requires recorded in that spec's verification ledger. Owner eugenelim", needs = [] },
  { path = "docs/specs/walking-skeleton-role-compilation/spec.md", kind = "defect", source = { mode = "repo-origin" }, summary = "ac-0261-names-a-type-nothing-appends: the criterion's literal step.failed token is residual wording. Its own clause requires the type to distinguish the event from a runtime fault, and the obligation row states the criterion with no type token, so role.load.failed and role.compile.refused satisfy it and no amendment is owed. The settled reading now sits in the spec's Follow-ons, adjudicated 2026-09-22. Residual: drop the token only if an amendment reopens the spec. Owner eugenelim", needs = [] },
  { path = "docs/architecture/role-configuration-seams/role-configuration-seams.md", kind = "defect", source = { mode = "repo-origin" }, summary = "Both halves of r5 section 4's arbitrary-code control are unowned in Phase 1: membership of the closed integration_registry.kind set, and the compile-time egress check on connection_ref. r5 pairs them as one control. walking-skeleton-role-compilation's migration creates the kind column and nothing in Phase 1 reads it, so a load-time criterion there would guard a field no code consumes; the egress half needs a proxy allowlist Phase 1 does not deploy, and walking-skeleton-authority-containment already places its compensating control in the AWS deployment. Both halves are therefore routed to the AWS deployment. Surfaced by the round-4 security review 2026-09-21. Owner eugenelim", needs = [] },
  { path = "tests/quarantine/test_label_vocabulary.py", kind = "defect", source = { mode = "repo-origin" }, summary = "label-vocabulary-check-named-for-a-binding-it-does-not-make: the check now named test_compiling_a_quarantined_role_beside_a_row_naming_the_token_does_not_widen_it passed integrations=(an_integration(tools=(STEERED_LABEL,)),) alongside a_quarantined_role(), whose ceiling is empty — so _bound_integrations iterates nothing, the row is never read, and the check passes identically with integrations=(). It could not fail for the reason its old name gave. Adjudicated 2026-09-22 as naming, not coverage: AC-0268 stays discharged by the three checks above it — the well-formed non-member refusal, every declared member admitted, and the parser carrying no parameter a record could arrive through. Closed here only as far as the name and docstring go, which now state that the compiler never reads the row. Residual, and the reason this entry stays open: the binding version, where a ceiling entry resolves the row so a guard actually reads it, is a second check and is not written. Owner eugenelim", needs = [] },
]
closed = [
  { path = "src/ced/domain/quarantine/mint.py", kind = "defect", source = { mode = "repo-origin" }, summary = "duplicate-identity-attributes-resolve-last-wins: closed 2026-09-22 by refusing the document. An ix:nonFraction element declaring name or contextRef more than once now raises UnmintableFactIdentity and the filing yields no candidate set; _identity() takes the attribute list rather than a mapping, because collapsing to a mapping is what hid the duplicate. Reason for that remedy over recording a precedence rule: a precedence rule is exactly what no ratified document settles, and writing one in the most lenient reader would settle it by fiat and let this mint resolve documents a conformant XBRL processor rejects. Reason over refusing only the element: a partial set reports itself complete. Taken together with mint-drops-an-unreadable-identity under one recorded rule, as that finding invited: a numeric fact element yields exactly one conforming identity, or the mint fails. Extended the same day, on the T4 reviews, to xsi:nil — the attribute that decides whether a fact is a candidate at all, and the sharper half of the same defect, since under last-wins a not-nil flag followed by a nil one dropped a well-formed fact in silence while the reversed order minted it. The rule is now stated as the closed set of attributes this reader interprets — name, contextRef, xsi:nil — and enforced in one place, _read_once(). A repeated attribute the mint does not read, such as unitRef, stays out of scope and mints normally. Mutation-proved separately for the identity attributes and for the nil flag. The recorded corpus contains no such element, so tests/fixtures/candidate_set_expected.json is unchanged and AC-0238 stays green. The walking-skeleton-role-compilation Follow-ons entry is rewritten in place to record the closure. Ships without an acceptance criterion by owner decision 2026-09-22. Owner eugenelim", needs = [] },
  { path = "migrations/env.py", kind = "defect", source = { mode = "repo-origin" }, summary = "no-lock-timeout-on-migrations: closed 2026-09-22 by setting a bounded wait rather than by accepting an unbounded one. env.py now sets lock_timeout to 5s for the migration session, via set_config so no value is interpolated into SQL text. Failure behaviour when it fires: the statement aborts with 55P03 lock_not_available, the single transaction env.py commits explicitly rolls the whole upgrade back, and the operator retries when the table is quiet; migrations are expand-only and re-runnable, and the error names contention rather than a schema fault. Reason for that over accepting the wait: a stalled deploy on a restart: no fleet needs a human, where a fast failure needs a retry. The value was fixed when the decision was first taken, under the rule the closed sets in vocabulary.py and roles.py follow; that was set aside the same day on the T4 quality review, which showed setting it after connecting overrides PGOPTIONS, ALTER ROLE and ALTER DATABASE alike, so a fixed value was not a default an operator could raise but a mandatory one with no route to a maintenance window except editing the file. A lock-wait budget is an operational knob whose right value depends on the table's traffic, which is what distinguishes it from those closed sets. So: 5s default, CED_MIGRATION_LOCK_TIMEOUT overrides, and a value resolving to 0 is refused because 0 disables the bound rather than setting it to no wait. Pinned by test_a_migration_blocked_by_a_reader_aborts_rather_than_queueing in tests/schema/test_migration_applies.py, which reproduces the whole scenario — probe database at 0002, a concurrent reader holding ACCESS SHARE, the migration aborting, the database still at 0002, and the retry applying once the reader closes. Its subprocess timeout is the assertion: without the bounded wait the run hangs on the reader rather than failing on a wrong value. An elapsed figure above the configured budget is expected and not a fault; lock_timeout applies per lock-acquisition attempt, so a run costs the budget times the number of lock-taking statements, plus alembic startup. Substrate checks hold it: the default under contention, the override against a reader retired on a clock that starts when the widening run does and asserting the run actually waited, the refusal of a disabling value in every spelling that resolves to zero, and that the refusal returns while a reader still holds the lock rather than after queueing on it. AGENTS.md § The local substrate carries the operator-facing guidance, including that a bare number is milliseconds. Revision 0003's docstring, which named this gap, was corrected in the same change. The round-3 verification ledger's dated record of the deferral is left as written. Owner eugenelim", needs = [] },
  { path = "src/ced/adapters/postgres/roles.py", kind = "defect", source = { mode = "repo-origin" }, summary = "tools-array-elements-unchecked: closed 2026-09-22. _check_integration_record now refuses a tools entry that is not a str, naming the row and the entry's position, in the shared guard both decode_role_record and list_integration_tools call. Refused rather than skipped, because a loader dropping the bad entry would shrink the tool surface silently, which is the defect AC-0273 closes one level up. Routing decision: the criterion this needs lands in neither this spec nor a successor. walking-skeleton-role-compilation is Shipped and its controlled-amendment transition is unavailable, and no successor spec owns the loader, so the owner chose on 2026-09-22 to ship the guard with no criterion and record that here. Residual: if that spec is ever reopened for an amendment on other grounds, this rule wants a criterion beside AC-0273. Two things were considered and left, both recorded so the omissions are choices rather than oversights. An empty or whitespace-only tool name still loads and still reaches AC-0233's enumeration as a tool, by the same route the closed defect describes; refusing it is a second rule and belongs with the criterion, not ahead of it. And ceiling is still checked for array-ness only; that defect has its own open entry on roles.py, because an unfixed consequence cannot live in a closed one. Mutation-proved: reverting the guard reds the new checks in tests/compiler/test_role_loader.py. Owner eugenelim", needs = [] },
  { path = "src/ced/domain/quarantine/mint.py", kind = "defect", source = { mode = "repo-origin" }, summary = "mint-drops-an-unreadable-identity: closed 2026-09-22 by the first of the three defensible remedies. An ix:nonFraction element declaring no name or no contextRef, or declaring either empty or valueless, now raises UnmintableFactIdentity exactly as a present-but-non-conforming identity does, and no candidate set is minted for the filing. Reason for that remedy over the other two: omitting an attribute is a cheaper route to an uncitable fact than misspelling one, so refusing only the misspelling leaves the wider half of the same steering channel open, and refusing the element rather than the mint would return a short set reporting itself complete. Enforced by _identity() in the mint's reader, over the shared _read_once() helper added when the rule was extended to xsi:nil. Mutation-proved: reverting the guard reds the new checks in tests/quarantine/test_mint_constrains_identities.py, except the nil-ordering one, which is green by design because it pins the nil read running first. The recorded corpus contains no such element, so tests/fixtures/candidate_set_expected.json is unchanged and AC-0238 stays green. Ships without an acceptance criterion by owner decision 2026-09-22. Owner eugenelim", needs = [] },
  { path = "docs/specs/walking-skeleton-role-compilation/plan.md", kind = "defect", source = { mode = "repo-origin" }, summary = "pre-existing-plan-stub-unformatted: `ruff format --check .` exits 1 on plan.md line 250, the AC-0202 red stub in T2's Tests field. ruff formats fenced Python in Markdown, and origin/main is clean, so this branch introduced the only failure the gate reports. Confirmed present at HEAD before any T1 code, and reproduced against `git show HEAD:...plan.md` in a scratch copy. plan.md is hash-pinned by the sealed baseline, so reformatting the block is a controlled amendment and excluding docs/ from the gate is a gate change; neither is the implementer's to take. Closed 2026-09-21 by an owner-authorized controlled amendment reformatting the stub; see docs/specs/walking-skeleton-role-compilation/notes/amendment-2026-09-21-stub-formatting.md. ruff format --check . now exits 0 repository-wide, and the plan stub is byte-identical to the materialized tests/compiler/test_stack_composition.py. Surfaced during T1 execution 2026-09-21. NOTE: backlog.closed cannot mechanically represent this one — the engine requires a closed defect's artifact to report Status: Closed with a resolution, and this artifact is a plan file whose vocabulary is Drafting/Approved/Executing/Done, so no value it can carry satisfies that rule. Recorded here rather than reshaped; the register-shape question is the owner's. Owner eugenelim", needs = [] },
]

# A Ready brief cannot sit in [backlog].open: `Ready` is terminal for kind
# "brief", so the schema reports impossible_transition. Its schema-valid home is
# an initiative's brief_queue, which is where this initiative comes from.
["ini-001"]
name   = "Inspectable Diligence MVP"
status = "active"

["ini-001".work]
# Phase 1 walking skeleton. Cut into three specs on 2026-09-18 after a single
# spec did not converge across three review rounds, then the agent-runtime spec
# was cut into three again on 2026-09-20 because it carried AC-0201 through
# AC-0232 — about two and a half times either sibling. The dependency chain is
# real: everything needs the schema and pool, authority-containment and
# step-lifecycle both need the compiler, and the evidence spec measures a system
# the other four build. The three runtime specs are a chain: step-lifecycle's
# resume criterion needs an approved tool body to run, which needs
# authority-containment's predicate.
queue = [
  { path = "docs/specs/walking-skeleton-role-compilation/spec.md", kind = "spec", source = { mode = "repo-origin" }, summary = "Phase 1 role compilation: framework seam, the role compiler, the four-layer toolset stack with the policy decision point outermost and fail-closed, and the quarantine boundary", needs = [{ type = "local", kind = "spec", path = "docs/specs/walking-skeleton-foundation/spec.md" }] },
  { path = "docs/specs/walking-skeleton-authority-containment/spec.md", kind = "spec", source = { mode = "repo-origin" }, summary = "Phase 1 authority containment: the containment fragment over interpreted arguments, and the policy decision point that refuses on argument value and records the denial", needs = [{ type = "local", kind = "spec", path = "docs/specs/walking-skeleton-foundation/spec.md" }, { type = "local", kind = "spec", path = "docs/specs/walking-skeleton-role-compilation/spec.md" }] },
  { path = "docs/specs/walking-skeleton-step-lifecycle/spec.md", kind = "spec", source = { mode = "repo-origin" }, summary = "Phase 1 step lifecycle: the scoped-role Bedrock call, the step deadline, suspension for approval, and persistence and resume from bytes alone", needs = [{ type = "local", kind = "spec", path = "docs/specs/walking-skeleton-foundation/spec.md" }, { type = "local", kind = "spec", path = "docs/specs/walking-skeleton-role-compilation/spec.md" }, { type = "local", kind = "spec", path = "docs/specs/walking-skeleton-authority-containment/spec.md" }] },
  { path = "docs/specs/walking-skeleton-evidence/spec.md", kind = "spec", source = { mode = "repo-origin" }, summary = "Phase 1 evidence: run state machine and publication, browser stream, the four Phase 1 measurements, analytical re-baseline, and the record of what was not established", needs = [{ type = "local", kind = "spec", path = "docs/specs/walking-skeleton-foundation/spec.md" }, { type = "local", kind = "spec", path = "docs/specs/walking-skeleton-role-compilation/spec.md" }, { type = "local", kind = "spec", path = "docs/specs/walking-skeleton-authority-containment/spec.md" }, { type = "local", kind = "spec", path = "docs/specs/walking-skeleton-step-lifecycle/spec.md" }] },
]
active = []
shipped = [
  { path = "docs/specs/walking-skeleton-foundation/spec.md", kind = "spec", source = { mode = "repo-origin" }, summary = "Phase 1 foundation: layout RFC, project gates, schema and both append paths, privilege split, API, worker pool and lease recovery", needs = [] },
]

["ini-001".brief_queue]
draft     = []
ready     = [
  { path = "docs/product/briefs/inspectable-diligence-mvp.md", kind = "brief", source = { mode = "repo-origin" }, summary = "Ready MVP brief coordinating the six Accepted foundation intents; non-dispatchable until a slice is cut", needs = [] },
]
executing = []
shipped   = []
withdrawn = []
cancelled = []
