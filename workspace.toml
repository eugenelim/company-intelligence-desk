# workspace.toml
#
# Repository coordination index. Canonical artifacts own requirements; this
# file records lifecycle membership, source provenance, display summaries, and
# hard dependencies.
#
# Target entries are inline tables with exactly:
#   path, kind, source, summary, needs
#
# Example:
#   { path = "docs/specs/<slug>/spec.md", kind = "spec", source = { mode = "repo-origin" }, summary = "Example spec", needs = [] }
#
# Comments, summaries, list order, tracker labels, and profile hints are
# non-semantic. They must not determine routing, dependency satisfaction,
# processor selection, or dispatch.
#
# Paths are repository-relative POSIX paths. Consumers must reject absolute
# paths, backslashes, ".." segments, and any symlink-resolved target outside the
# repository root.

[backlog]
open = [
  { path = "docs/product/intents/evidence-backed-company-diligence.md", kind = "intent", source = { mode = "repo-origin" }, summary = "Evidence-linked public-company change analysis as of an explicit date; architecture designed, awaits sign-off", needs = [] },
  { path = "docs/product/intents/multi-workspace-inspectable-experience.md", kind = "intent", source = { mode = "repo-origin" }, summary = "Distinct React workspace views over research, workflow, evidence, context, and policy; architecture designed, awaits sign-off", needs = [] },
  { path = "docs/product/intents/portable-identity-first-runtime.md", kind = "intent", source = { mode = "repo-origin" }, summary = "Portable containerized ADK runtime reaching Bedrock via short-lived workload identity; architecture designed, awaits sign-off", needs = [] },
  { path = "docs/product/intents/scoped-context-and-evidence.md", kind = "intent", source = { mode = "repo-origin" }, summary = "Bounded versioned context packages and immutable evidence lineage; architecture designed, awaits sign-off", needs = [] },
  { path = "docs/product/intents/governed-observable-and-evaluable-operation.md", kind = "intent", source = { mode = "repo-origin" }, summary = "Policy, telemetry, evaluation, untrusted-content boundary; boundary awaits design sign-off, rest awaits an observability companion", needs = [] },
  { path = "docs/product/intents/adoptable-reference-implementation.md", kind = "intent", source = { mode = "repo-origin" }, summary = "Legible, learnable reference implementation whose patterns transfer without the domain; awaits an owner pass naming the pattern set", needs = [] },
  { path = "docs/product/briefs/inspectable-diligence-mvp.md", kind = "brief", source = { mode = "repo-origin" }, summary = "Draft MVP brief coordinating the six foundation intents; blocked on charter ratification; architecture designed, awaits sign-off", needs = [] },
]
closed = []
