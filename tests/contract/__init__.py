"""The framework-seam contract suite.

Every module here asserts one row of
`docs/specs/walking-skeleton-role-compilation/plan.md` § Grounding probe
against the pinned `pydantic-ai-slim` release. A version bump that changes a
seam this design rests on reds here rather than in a running step.
"""
